import{Router}from"express";import{db,customersTable,invoicesTable,agenciesTable}from"@workspace/db";import{eq,and,ilike,sql,desc,gte,lte}from"drizzle-orm";import{requireAuth,AuthRequest}from"../middlewares/authMiddleware";
const router=Router();router.use(requireAuth as any);const statusFor=(balance:number,bill:number,due:string)=>balance<=.001?"paid":balance<bill-.001?"partial":due<new Date().toISOString().slice(0,10)?"overdue":"pending";
const fields={id:invoicesTable.id,customerId:invoicesTable.customerId,customerName:customersTable.name,agencyId:invoicesTable.agencyId,agencyName:agenciesTable.name,invoiceNumber:invoicesTable.invoiceNumber,invoiceDate:invoicesTable.invoiceDate,billAmount:invoicesTable.billAmount,dueDate:invoicesTable.dueDate,outstandingBalance:invoicesTable.outstandingBalance,status:invoicesTable.status,createdAt:invoicesTable.createdAt};
router.get("/",async(req:AuthRequest,res)=>{try{const{search,status,customerId,fromDate,toDate}=req.query,c:any[]=[];if(customerId)c.push(eq(invoicesTable.customerId,Number(customerId)));if(status)c.push(eq(invoicesTable.status,String(status)));if(fromDate)c.push(gte(invoicesTable.invoiceDate,String(fromDate)));if(toDate)c.push(lte(invoicesTable.invoiceDate,String(toDate)));const rows=await db.select(fields).from(invoicesTable).innerJoin(customersTable,eq(invoicesTable.customerId,customersTable.id)).leftJoin(agenciesTable,eq(invoicesTable.agencyId,agenciesTable.id)).where(and(...c,search?ilike(sql`concat(${customersTable.name},' ',coalesce(${agenciesTable.name},''),' ',${invoicesTable.invoiceNumber})`,`%${search}%`):undefined)).orderBy(desc(invoicesTable.invoiceDate));res.json(rows.map(r=>({...r,billAmount:Number(r.billAmount),outstandingBalance:Number(r.outstandingBalance)})))}catch(err){req.log?.error({err});res.status(500).json({error:"Failed to fetch invoices"})}});
router.post("/",async(req:AuthRequest,res)=>{try{const{customerId,agencyId,invoiceNumber,invoiceDate,billAmount,dueDate}=req.body;if(!customerId||!agencyId||!invoiceNumber||!invoiceDate||!dueDate||Number(billAmount)<=0){res.status(400).json({error:"Store, agency and valid bill details are required"});return}const[invoice]=await db.insert(invoicesTable).values({customerId:Number(customerId),agencyId:Number(agencyId),invoiceNumber:String(invoiceNumber).trim(),invoiceDate,billAmount:String(billAmount),dueDate,outstandingBalance:String(billAmount),status:statusFor(Number(billAmount),Number(billAmount),dueDate)}).returning();res.status(201).json({...invoice,billAmount:Number(invoice.billAmount),outstandingBalance:Number(invoice.outstandingBalance)})}catch(err){req.log?.error({err});res.status(500).json({error:"Failed to create invoice"})}});
router.get("/:id",async(req:AuthRequest,res)=>{try{const[row]=await db.select(fields).from(invoicesTable).innerJoin(customersTable,eq(invoicesTable.customerId,customersTable.id)).leftJoin(agenciesTable,eq(invoicesTable.agencyId,agenciesTable.id)).where(eq(invoicesTable.id,Number(req.params.id)));if(!row){res.status(404).json({error:"Invoice not found"});return}res.json({...row,billAmount:Number(row.billAmount),outstandingBalance:Number(row.outstandingBalance)})}catch(err){req.log?.error({err});res.status(500).json({error:"Failed to fetch invoice"})}});
router.patch("/:id",async(req:AuthRequest,res)=>{try{const id=Number(req.params.id),[old]=await db.select().from(invoicesTable).where(eq(invoicesTable.id,id));if(!old){res.status(404).json({error:"Invoice not found"});return}const paid=Number(old.billAmount)-Number(old.outstandingBalance),bill=req.body.billAmount===undefined?Number(old.billAmount):Number(req.body.billAmount),due=req.body.dueDate??old.dueDate;if(!Number.isFinite(bill)||bill<=0||bill+ .001<paid){res.status(400).json({error:"Bill amount cannot be less than already paid amount"});return}const balance=Math.max(0,bill-paid),updates:any={billAmount:String(bill),outstandingBalance:String(balance),status:statusFor(balance,bill,due)};for(const key of["invoiceNumber","invoiceDate","dueDate"]){if(req.body[key]!==undefined)updates[key]=req.body[key]}if(req.body.agencyId!==undefined)updates.agencyId=Number(req.body.agencyId)||null;const[updated]=await db.update(invoicesTable).set(updates).where(eq(invoicesTable.id,id)).returning();res.json({...updated,billAmount:Number(updated.billAmount),outstandingBalance:Number(updated.outstandingBalance)})}catch(err){req.log?.error({err});res.status(500).json({error:"Failed to update invoice"})}});
router.delete("/:id",async(req:AuthRequest,res)=>{try{await db.delete(invoicesTable).where(eq(invoicesTable.id,Number(req.params.id)));res.json({success:true})}catch(err){req.log?.error({err});res.status(500).json({error:"Failed to delete invoice"})}});export default router;
